FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package.json ./

# Install dependencies
RUN npm install

# Copy the rest of the application code
COPY . .

# Expose the development port
EXPOSE 5173

# Start the development server with host flag to make it accessible outside the container
CMD ["npm", "run", "dev", "--", "--host"] 